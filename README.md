# Night_vision_motionsensor
Accurate OpenCV motion detector – MOG2 background subtraction, ignores shadows/noise, night-vision tint + audible alerts
Note this is only for mac os and linux users to operate this --
Linux/macOS alternative (uncomment + pip install playsound + beep.wav file)
from playsound import playsound
def beep():
playsound("beep.wav")

# Globals
